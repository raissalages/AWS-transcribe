 All right, so now let's take a look at cloudwatch alarm. So cloudwatch alarms monitors a cloudwatch metric based on a defined threshold. Okay, so there we have an alarm. And so the idea here is that when the alarm is breached, it's going outside the defined threshold, and then it changes state, okay. And so for metric alarm state, we have Okay, we have alarm, and we have insufficient data. Okay, so there's those three different alarms. So when it changes state, then we can define what action it should trigger. And so what we can do here is choose what we want it to do. So we can send out a notification via s&s, it can trigger an auto scaling group action or can trigger an EC two action such as shutting down a server. Okay. All right, so now let's