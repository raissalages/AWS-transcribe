 aliases allow you to give specific versions of a friendlier name when accessing the lambda programmatically. So the idea is we can create a lambda provided a name, specify that specific version. And so now we can see that we have the stable version. That makes sense why we would not be able to use aliases with unqualified because unqualified would be basically as they show the version like it'd be the version there. Right. So that's as simple as what aliases are. Yep. Let's take a look here.