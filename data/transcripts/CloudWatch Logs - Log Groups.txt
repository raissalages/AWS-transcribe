 Now let's take a look at CloudWatch logs log group. So a log group is a collection of log streams. And it's a common to name the log groups with the forward slash syntax, which we'll see here in a moment. So here, if you were to open up CloudWatch, you'd have log groups, and you can go ahead and create a log, you would name it using that forward slash syntax. And so it would then go appear here. The reason you'd want to do that is then you could scope it based on maybe your production environment or whatever convention that you want to use. You can see that the retention is never expire. But you can change that from never expire to 120 months here, which we can see here, which is 10 years. And that's log groups. So we were just